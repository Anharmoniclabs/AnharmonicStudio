# Baseline Audit: 2026-09-16

## Repository identity

- Repository: `Anharmoniclabs/AnharmonicStudio`
- Branch: `main`
- Starting SHA: `8ff5651d90bc737edc4bbbbf28f0ef7deffd11f1`
- `origin/main`: `8ff5651d90bc737edc4bbbbf28f0ef7deffd11f1`
- Worktree: clean before this document was added
- Files in the checkout: 972
- Python files in `mpclab`, `tests`, `delivery`, `scripts`, and `plugins`: 368
- Native files: 176
- Browser JavaScript, HTML, and CSS files under `website` and `delivery`: 17

## Environment

- OS: Ubuntu 24.04.4 LTS dev container
- Python: 3.14.2 (`/home/codespace/.python/current/bin/python`)
- `uv`: unavailable
- `ruff`: unavailable
- `pytest` module: unavailable to the selected Python interpreter
- Node.js and npm: available
- CMake: available
- GitHub CLI: available

The repository requires Python 3.12. The missing locked toolchain and project
dependencies mean that unavailable checks below are environment-blocked, not
classified as application failures.

## Commands and results

| Command | Result | Classification |
| --- | --- | --- |
| `git fetch origin main` | Pass; `origin/main` matches starting SHA | Baseline |
| `python -m compileall -q mpclab delivery scripts tests` | Pass | Existing behavior |
| `uv run --no-sync ruff check mpclab tests scripts` | Not run; `uv` unavailable | Environment |
| `uv run --no-sync ruff format --check mpclab tests scripts` | Not run; `uv` unavailable | Environment |
| `python -m pytest -q ...` focused recording selection | Not run; `pytest` module unavailable | Environment |
| `python scripts/check_website.py` | Not run; project runtime dependencies unavailable | Environment |
| Full Python/Qt suite | Not run locally; Python 3.12 environment and dependencies unavailable | Environment |
| Native C++ tests | Not run locally; dependency/build baseline not provisioned | Environment |
| Browser JavaScript tests | Not run locally; locked browser setup not provisioned | Environment |
| Chromium tests | Not run locally; Playwright setup not provisioned | Environment |
| Website validator | Not run locally; project runtime unavailable | Environment |
| Packaged/offscreen startup | Not run locally; project runtime unavailable | Environment |
| Linux bundle test | Not run locally; packaging dependencies unavailable | Environment |

## GitHub evidence at the starting SHA

Open PR #3 is titled “Separate vocal, audio-input and MIDI capture paths”. Its
native matrix checks and Linux bundle checks are failing; its web checks are
successful and its test checks are cancelled. Recent Actions runs at the same
SHA show failures for Source checks, Portable C++ engine, and Publish studio
website. The Linux bundle failure is tracked independently from the local
toolchain blockage until its job logs are inspected.

## Known source anchors

The requested lint blockers are present in the starting tree:

- `mpclab/engine_scheduling.py` imports `ScheduledNote` twice and contains the
  reported unused loop variable.
- `mpclab/external_dsp.py` references `voice` before it is defined in
  `ExternalDSP.note_on`.
- `tests/test_transcription.py` imports `Row` without using it.
- The repository contains `zip()` calls that must be checked for explicit
  `strict=` under the active Ruff configuration.

These are the first repair tranche. No architectural file movement has been
performed.

## Required follow-up

1. Provision Python 3.12 with the locked dependencies or use CI logs for the
   unavailable quality gates.
2. Inspect the failing PR #3 and current Actions job logs, especially the Linux
   bundle failure.
3. Repair and test the source-check blockers before architectural movement.